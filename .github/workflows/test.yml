name: Test
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: ./
        with:
          expected: |
            { "a" : 1 }
          actual: |
            {"a":1}
